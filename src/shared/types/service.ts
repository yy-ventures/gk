export interface IService {
    id: number
    title: string
    image: string
}

export interface IServiceDetails {
    id: number
    title: string
    subtitle: string
    description: string
    image: string
}
